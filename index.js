/* Borrar solamente uno */
function borrar(text) {
    let cadena = text;
    let cadenaCorregida = cadena.substring(0, cadena.length - 1);
    return cadenaCorregida
}
/* Hacer la raiz cuadrada */
function raizCuadrada(text) {
    return Math.sqrt(text)
}